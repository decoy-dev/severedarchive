# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Fetched/synthesized media pipeline sources (not committed; run scripts/fetch-media.sh)
raw/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/shots/

# `wrangler dev` local state: Miniflare's KV and cache, as SQLite. Local only,
# machine-specific, and rewritten constantly while the Worker runs.
.wrangler/

# `wrangler dev` local secrets. Real secret values for local runs — never commit
# one, and never point the production deploy at this file.
.dev.vars
.dev.vars.*

# Agent tooling (local only — installed skills, harness config, MCP scratch)
.agents/
.claude/
.playwright-mcp/
skills-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
